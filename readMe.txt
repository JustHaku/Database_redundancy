The Compile and run.bat creates a replica named Replica.txt with the data from the database.

SQLiteJDBC acts as the main driver for the program and the dbName value should be changed to the name of the database to replicate.

Using the following command will run the programs main driver class: java -classpath ".;sqlite-jdbc-3.7.2.jar" SQLiteJDBC.


